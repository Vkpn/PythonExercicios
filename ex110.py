from utilidadeCeV import moeda
p = float(input('Digite o preço do produto: R$'))
poa = float(input('Digite a porcentagem de aumento: '))
por = float(input('Digite a porcentagem de redução: '))
moeda.resumo(p, poa, por)
